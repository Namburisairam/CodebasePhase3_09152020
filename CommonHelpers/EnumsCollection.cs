namespace CommonHelpers
{
    public enum enPlatformTypes
    {
       Web,
       Android,
       IOS,
    }
    public enum enQRCodeFor
    {
        Event = 0,
        Activity = 1,
        Sponsor = 2,
        Attendee,
        Exhibitor,
        Vendor
    }
    public enum enUserRole
    {
        Admin,
        EventManager,
        Moderator
    }
}
